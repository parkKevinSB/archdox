package com.archdox.cloud.engine.mcp;

public enum McpJsonRpcErrorCategory {
    METHOD_NOT_FOUND,
    TOOL_NOT_FOUND,
    INVALID_PARAMS,
    SCOPE_REQUIRED,
    FORBIDDEN,
    QUOTA_EXCEEDED,
    NOT_FOUND,
    ARCHDOX_API_ERROR,
    INTERNAL_ERROR
}
