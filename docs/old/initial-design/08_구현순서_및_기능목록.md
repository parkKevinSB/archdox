# ArchDox 상세 설계 — 구현 순서 & 기능 목록

`건축자동화_개발구현계획.md` §13의 phase 구조를 유지하면서 멀티테넌트/이미지/PWA/Admin 요건을 반영하여 확장한다.

## Phase 0. 저장소 뼈대 (1주)

| # | 항목 |
|---|---|
| 0.1 | Monorepo 구조 생성 (pnpm workspace + Maven multi-module) |
| 0.2 | 모듈: `cloud-api`, `archdox-agent`, `domain-shared`, `client/web`, `client/mobile`, `admin`, `ai-service`, `infra` |
| 0.3 | Java 21 + Spring Boot 3.5, Flower/Bloom 의존 등록 |
| 0.4 | docker-compose: postgres, mailhog, minio(S3 mock), libreoffice |
| 0.5 | CI: GitHub Actions (build, test, lint) |
| 0.6 | abyss-runner의 `FlowerBloomConfiguration` 이식 |

## Phase 1. Cloud API — 인증·테넌시·기본 도메인 (2~3주)

| # | 항목 | 산출 |
|---|---|---|
| 1.1 | `users`, `offices`, `office_memberships` 엔티티 + Flyway | DB |
| 1.2 | 자체 JWT 인증 + refresh token | `auth` 패키지 |
| 1.3 | 회원가입 / 로그인 / 로그아웃 / 비밀번호 reset | API |
| 1.4 | 개인 사용자 가입 시 `personal-*` office 자동 생성 | use case |
| 1.5 | 사무소 생성 / 멤버 초대 / 역할 변경 | API |
| 1.6 | `X-Office-Id` 헤더 + Membership filter | Spring Security filter |
| 1.7 | `projects`, `inspection_reports`, `inspection_report_steps`, `inspection_checklist_items` | DB+API |
| 1.8 | report draft 생성 / 단계 저장 / 제출 / 취소 + draft 저장 모드(`CLOUD_ENCRYPTED`/`LOCAL_ONLY`) | API |
| 1.9 | `audit_logs` 기록 (모든 mutating action) | aspect |
| 1.10 | 멀티테넌트 격리 자동 테스트 (cross-office 접근 → 차단) | 통합 테스트 |

## Phase 2. 사진 파이프라인 (2주)

| # | 항목 |
|---|---|
| 2.1 | `photos` 엔티티 + storage_ref logical key 규칙 |
| 2.2 | `/photos/intent` API — 기본 target은 `CLOUD_MEDIATED`, 개인은 `S3`, 직접 업로드는 `ARCHDOX_AGENT_DIRECT` 선택 옵션 |
| 2.3 | S3 presigned POST/PUT 발급 (개인 플랜 + Cloud-mediated temp 업로드) |
| 2.4 | 썸네일 전용 S3 bucket + CloudFront signed URL |
| 2.5 | `/photos/{id}/confirm` |
| 2.6 | 클라이언트 worker 라이브러리: resize + EXIF strip + hash |
| 2.7 | 중복 hash dedup |
| 2.8 | ArchDox Agent 직접 업로드 signed token 검증 (mTLS는 Agent↔Cloud만) |
| 2.9 | 일/월 업로드 한도 (`feature_usage_counters`) + 429 처리 |

## Phase 3. ArchDox Agent MVP (3주)

| # | 항목 |
|---|---|
| 3.1 | Spring Boot ArchDox Agent 부트스트랩, packaging (`jpackage` Windows MSI) |
| 3.2 | Pairing wizard (`install_token` → mTLS cert 발급) |
| 3.3 | Heartbeat (60s) |
| 3.4 | Commands long-poll (`/agent/api/v1/commands/next`) |
| 3.5 | SQLite 로컬 store |
| 3.6 | NAS adapter (SMB / NFS / Local) + 자체 진단 |
| 3.7 | Photo pickup command 처리 (Cloud-mediated temp S3 → NAS 저장) |
| 3.8 | Photo direct upload endpoint (선택 옵션) — Cloud signed upload token 검증 |
| 3.9 | Photo NAS 저장 + 썸네일 생성 fallback (클라이언트가 못 했을 때) |
| 3.10 | Cloud API: `archdox_agents`, `archdox_agent_commands`, `archdox_agent_heartbeats` 처리 |
| 3.11 | Admin이 `install_token` 발급하는 API + 사무소 IT가 페어링 |

## Phase 4. 문서 생성 Flow + 전달 정책 (Cloud + Local) (3~4주)

| # | 항목 |
|---|---|
| 4.1 | `document_jobs`, `document_artifacts`, `document_delivery_requests` 엔티티 |
| 4.2 | `DocumentGenerationOrchestrationFlow` (Cloud) — abyss-runner `DeathResolveFlowFactory` 패턴 |
| 4.3 | `DocumentRenderFlow` (Local) — docx4j 템플릿 바인딩 + LibreOffice PDF |
| 4.4 | 사진 grouping (체크리스트 항목별 2x2) |
| 4.5 | 완료 콜백 → `DocumentGeneratedEvent` Bloom 발행 |
| 4.6 | SSE `/events/stream`로 진행 상태 push |
| 4.7 | `LOCAL_INBOX` / `EMAIL_LINK` / `LOCAL_PRINT` 전달 요청 API (`12` 문서) |
| 4.8 | 실패 retry / cancel 정책 |
| 4.9 | 통합 테스트: 제출 → command pickup → 생성 → completion → GENERATED |

## Phase 5. Client (PWA) (4주, Phase 1~4와 일부 병행)

| # | 항목 |
|---|---|
| 5.1 | React + Vite + Tailwind + shadcn 셋업, PWA manifest/SW |
| 5.2 | 로그인 / 회원가입 |
| 5.3 | 사무소 선택 (1개면 자동) |
| 5.4 | 프로젝트 목록 / 생성 |
| 5.5 | 감리일지 wizard (5 steps) |
| 5.6 | 체크리스트 + 사진 첨부 UX |
| 5.7 | 사진 촬영 → resize → 업로드 큐 |
| 5.8 | 오프라인 임시 저장 (IndexedDB) + 재동기화 |
| 5.9 | 리포트 상태 / 진행률 / 다운로드 차단 정책 표시 |
| 5.10 | 알림 / 설정 |
| 5.11 | Capacitor 래핑 — iOS / Android 빌드 |

## Phase 6. 정기 안전점검 템플릿 + Regulation MVP + Template Studio MVP (3~4주)

| # | 항목 |
|---|---|
| 6.1 | 정기점검 schema 정의 (관련 법령 양식 기반) |
| 6.2 | 템플릿 DOCX 작성 |
| 6.3 | 클라이언트 wizard 추가 |
| 6.4 | 생성 flow 재사용 (template_id만 다름) |
| 6.5 | `regulation_sources`, `regulation_articles`, `review_reports`, `review_findings` 엔티티 |
| 6.6 | 핵심 법령 seed + template schema의 `regulationRefs` / `validations` |
| 6.7 | RuleEngine 5종 (Required/MutuallyExclusive/MinPhotoCount/DateOrder/NumericRange) |
| 6.8 | 단계 저장/제출 시 룰 검토 + Layer A/B UI ((i) 아이콘, 관련 법령 패널) |
| 6.9 | Template Studio MVP: DOCX 업로드 → placeholder 추출 → schema 매핑 → preview |

## Phase 7. Admin Console (3주)

| # | 항목 |
|---|---|
| 7.1 | `admin_users`, TOTP, audit |
| 7.2 | 테넌트 관리 화면 |
| 7.3 | Agent 모니터링 대시보드 |
| 7.4 | 사용량 / 라이선스 화면 |
| 7.5 | 공지 broadcast |
| 7.6 | 감사 로그 검색 / export |
| 7.7 | 시스템 기본 템플릿 관리 |
| 7.8 | 법령 카탈로그 관리 / 갱신 이력 |
| 7.9 | 전달 이력(`document_delivery_requests`) 조회 |

## Phase 8. AI 보조 분류 / 법령 AI 검토 (3~4주, 데이터 모이면)

| # | 항목 |
|---|---|
| 8.1 | Python FastAPI service |
| 8.2 | `PhotoClassificationFlow` |
| 8.3 | 추천 top-3 + 사용자 확정 UX |
| 8.4 | `photo_label_feedback` 수집 |
| 8.5 | `ReviewFlow`의 AI reviewer 단계 (관련 법령 top-K + JSON finding) |
| 8.6 | Layer C/D UX (인라인 인디케이터 + 제출 전 검토 보고서) |
| 8.7 | 사후 재학습 batch (별도 노트북 워크플로) |

## Phase 9. Supervisor 확장 / 운영 강화 (4주)

| # | 항목 |
|---|---|
| 9.1 | AgentOfflineWatchdog Flow |
| 9.2 | 알림 채널 다양화 (push, SMS) |
| 9.3 | 자동 백업 가이드 + agent.sqlite export |
| 9.4 | Stripe/PG 결제 연동 |
| 9.5 | 사무소 export / purge job |
| 9.6 | Redis 도입 → distributed lock, SSE backbone |
| 9.7 | 다중 ECS task 확장 |
| 9.8 | ArchDox Agent 자동 업데이트 (allowlist command) |

## Phase 10. 장기 / 옵션

- Supervisor 별도 서비스 분리
- SSO/SAML
- Cloudflare Tunnel 자동화로 사무소 NAT 환경 단순화
- HWP/HWPX 자동화 (Windows COM helper, C#)
- 세움터/e행정 인터페이스 검토 (보안 리스크 고)
- 다국어 (en/ja)

## 부가 — 전체 기능 카탈로그 (체크리스트)

### 인증/계정
- [ ] 이메일+비번 회원가입
- [ ] 이메일 인증
- [ ] 비밀번호 재설정
- [ ] 소셜 로그인 (선택)
- [ ] 자체 JWT + refresh rotation
- [ ] 계정 탈퇴 / 데이터 export

### 사무소/멤버십
- [ ] 사무소 생성/수정/해지
- [ ] 멤버 초대/역할 변경/제외
- [ ] 멤버십 N:M (다중 사무소)
- [ ] 사무소 전환 UI
- [ ] 개인 자동 사무소

### 프로젝트
- [ ] CRUD
- [ ] 아카이브
- [ ] 멤버별 접근 권한 (project_members, 추후)

### 리포트
- [ ] 감리일지 draft
- [ ] 단계별 저장 / 자동 저장
- [ ] draft 저장 모드 (`CLOUD_ENCRYPTED` / `LOCAL_ONLY`)
- [ ] 체크리스트 (사진 첨부 가능)
- [ ] 특이사항 / 지시사항
- [ ] 제출 / 취소
- [ ] 재생성 / 재시도
- [ ] 정기 안전점검

### 사진
- [ ] 카메라 촬영 (PWA)
- [ ] 갤러리 업로드
- [ ] 클라이언트 resize / EXIF strip
- [ ] 썸네일 자동 생성
- [ ] 업로드 큐 + 재시도
- [ ] AI 추천 라벨
- [ ] 사용자 확정 라벨
- [ ] 사무소 내 dedup

### 문서 생성
- [ ] Cloud generator (개인 플랜)
- [ ] ArchDox Agent generator (사무소 플랜)
- [ ] DOCX 템플릿 바인딩
- [ ] PDF 변환
- [ ] 프린터 출력 (ArchDox Agent)
- [ ] 전달 요청 (`LOCAL_INBOX` / `EMAIL_LINK` / `LOCAL_PRINT`)
- [ ] 이메일/임시 링크 발송 이력 감사

### 템플릿 / 법령
- [ ] DOCX placeholder 추출
- [ ] schema field 매핑
- [ ] compose policy / prompt 설정
- [ ] 샘플 데이터 preview
- [ ] 핵심 법령 catalog seed
- [ ] field ↔ regulationRefs 매핑
- [ ] RuleEngine 저장/제출 검토

### 로컬 에이전트
- [ ] 페어링
- [ ] mTLS
- [ ] heartbeat
- [ ] long-poll command pickup
- [ ] NAS adapter
- [ ] Cloud-mediated photo pickup
- [ ] direct upload signed token 검증
- [ ] 로컬 관리 UI
- [ ] 백업 / 진단 도구

### Admin
- [ ] 별도 인증 + TOTP
- [ ] 테넌트 관리
- [ ] 에이전트 모니터링
- [ ] 사용량 / 라이선스
- [ ] 공지 / 알림
- [ ] 감사 로그
- [ ] 템플릿 관리

### 보안 / 운영
- [ ] audit_logs 전체 mutating action 기록
- [ ] RLS (운영 단계)
- [ ] CloudFront signed URL
- [ ] presigned URL 만료 정책
- [ ] 비용 모니터링 알람
- [ ] 백업 정책

## 클로드에게 주는 첫 PR 단위 권장

> "Phase 0 + Phase 1.1~1.6 까지를 첫 PR로 만든다. 즉, 저장소 뼈대 + 인증/사무소/멤버십 + 멀티테넌트 격리 테스트가 첫 마일스톤이다. 사진/문서 생성은 다음 PR."
